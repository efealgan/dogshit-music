uses discord.js, @discordjs/voice, yt-dlp-exec, dotenv node.js libraries.

uses ffmpeg externally. -> needs it installed on your system. on windows, add ffmpeg directory to your PATH.

pulls lyrics from LRCLIB.
